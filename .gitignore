# dependencies
node_modules/

# secrets - NEVER commit the real Decart key
.env
.env.local

# logs / os
npm-debug.log*
*.log
.DS_Store
Thumbs.db
.vercel

# local session store (test data - not committed)
sessions.json
data/
.env*

# visual-QA harness - both are GENERATED (test/e2e/fixtures.mjs) and large.
# Regenerated on demand by the spec, so a fresh clone needs no extra setup.
test/fixtures/
test-results/
playwright-report/
